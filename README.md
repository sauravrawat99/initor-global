# Initor Global — Full Stack Website

## Tech Stack
- **Frontend:** React 18, Vite, Tailwind CSS, React Router v6
- **Backend:** Node.js, Express.js, Nodemailer

## Setup

### Client
cd client && npm install && npm run dev

### Server
cd server && npm install && node server.js